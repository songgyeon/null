# NULL blankware assets

Fresh, decodable PNG replacements for the opening, enrollment, messenger,
map, hidden, gift, camera, bag, and sticker surfaces.

All files are generated at 1024×1024 RGBA by
`tools/build-blankware-assets.py`. Re-run the script to rebuild the complete
set without relying on the damaged Library ZIP archives.
