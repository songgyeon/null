# NULL

**당신이 말하지 않아도, 세계는 돌아갑니다.**

AI 캐릭터 관계 시뮬레이터. 유저는 한 달 뒤 떠나는 교생이고, 두 캐릭터는 유저가 없는 사이에도 서로 대화합니다.

🔗 **[songgyeon.github.io/null](https://songgyeon.github.io/null/)** · 웹 · Android(Expo)

기획 · 서사 · 캐릭터 설계 · 개발 단독 수행 — 문리현

```
node test/run.mjs                   # 2235개 회귀 테스트. 의존성·네트워크·API 키 없이 돈다
node test/engine-pipeline.test.mjs  # 생성 경로 회귀 418개 (G 네 갈래 + G2 모델 스윕 + G3 sonnet5-pair-haiku + G4 single5 + G5 golden-v1 + §13 계약 복구 + §14 기본 배선) — 역시 키 없이 돈다
node tools/eval.mjs                 # 대사 품질 자 — 내보낸 기록에서 명백한 것을 센다
node tools/replay.mjs --fake        # 네 갈래 replay 하네스 자체 점검 (실제 재생은 ANTHROPIC_API_KEY로)
node tools/selected-replay.mjs --fake  # 운영 기본 경로(깃발 없음) 재생 점검
```

```
worker.js             # Cloudflare Worker — 프롬프트 조립·캐싱·파싱·보안
index.html            # 웹 — 뼈대만. 아래 넷을 순서대로 싣는다 (React 18, 빌드 없음)
null.css              #   전부의 생김새
app-data.js           #   데이터와 규칙 — 인물·방·선물·장소·시간표. JSX가 없어서 바벨을 안 탄다
app-ui.js             #   화면 조각 — 아이콘·창·방 목록·채팅방
app.js                #   앱 — 상태를 들고 저장소를 읽고 워커를 부른다
app/App.tsx           # Android (React Native / Expo)
app/lib/rules.ts      #   ★ app-data.js에서 만들어진다 — 웹과 앱이 같은 규칙을 읽는다
app/lib/shim.ts       #   그 규칙이 딛는 바닥(localStorage·location)을 앱에 만들어 준다
app/lib/flow.ts       #   자리 판단 — 웹 app.js의 사다리를 순수 함수로
app/screens/          #   캐비닛 지도 · 창들(자리·나가기·귀갓길·명패·단톡·문틈)
app/lib/              # api.ts(호출) · profiles.ts(단계·BGM) · db.ts(expo-sqlite)
app/assets/           # 앱에 묶어 넣는 그림 — 아이콘 3장 + 비눗방울·자막막·포인터
app/assets/fonts/     # Galmuri11 + OFL 전문. 없으면 번들이 그 자리에서 멈춘다
app/app.json          # 패키지·아이콘·플러그인
app/eas.json          # APK 빌드 설정
app/index.ts          # 진입점. registerRootComponent 한 줄
app/package.json      # SDK 54 의존성. expo-asset·expo-constants가 명시로 박혀 있다
app/package-lock.json # 트리 고정 — 이게 없으면 npm이 SDK를 안 보고 최신을 끌어온다
docs/dialogue-corpus.md  # 문구집 원본 — 488개 의도 · 2,047줄. 손으로 고치는 건 여기뿐
docs/status-messages.md  # 상태메시지 — 쓰는 것과 안 쓴 것
docs/playlog-review.md   # 플레이 기록 검수 — 화면에 나오면 안 되는 것, 메아리 133회
docs/playlog/            # 검수한 기록 원본
docs/art-direction.md    # 그림 방향 — 왜 서른아홉으로 보였나, 무엇을 몇 장 다시 뽑나
docs/art-prompts.md      # 재언 재생성 프롬프트 33장 — BASE + 장별 SCENE
tools/demo-engine.js     # 매칭 엔진 — 웹·앱 결과물에 그대로 이어 붙는다
tools/build-demo.mjs     # 문구집 → demo-lines.js + app/lib/demoLines.ts
tools/build-rules.mjs    # app-data.js → app/lib/rules.ts (규칙은 손으로 안 베낀다)
tools/eval.mjs           # 품질 자 — 상담사 말투·메아리·같은 말 반복·옛 정사를 센다
docs/engine-plan.md      # 생성 엔진 — 모델 배치와 그 결정이 코드 어디에 사는지
test/run.mjs          # 회귀 테스트
cab-icons/            # 캐비닛 지도 — 프레임·문짝 열림/잠김·명패·TV
*.webp                # 배경·사진·프로필 112장 — 인물 63, 자리·물건·선물 39, 히든 문서 8, 바탕화면 2
null-logo.mp3         # 오프닝 로고곡
```

웹은 `index.html`을 웹서버로 열면 돕니다 — 갈라진 네 파일을 가져오느라 `file://`로는
안 돕니다(`python3 -m http.server`면 충분합니다). 빌드 단계는 없습니다: JSX는
브라우저에서 Babel standalone이 그 자리에서 컴파일합니다. 앱은 `cd app && npm install && npx expo start`,
APK는 `npx eas-cli build -p android --profile preview`(Expo 로그인 필요).

앱 화면 글꼴은 Galmuri11(SIL OFL 1.1)입니다. `app/assets/fonts/`에 글꼴과 라이선스 전문이
같이 들어 있습니다 — OFL이 재배포할 때 전문을 같이 두라고 요구합니다.

사진은 WebP입니다. 원래 PNG였는데 53장 합계가 103MB라 배경 한 장 뜨는 데 2MB를 받았습니다.
같은 해상도 그대로 WebP q82로 바꿔 **4.5MB**가 됐습니다(23배). 1:1로 확대해 비교했고 눈에 띄는 차이는 없었습니다.

---

## 왜 만들었나

기존 AI 캐릭터 챗봇은 전부 **유저 ↔ 캐릭터 1:1** 구조입니다. 유저가 입력을 멈추면 세계도 멈춥니다.

NULL은 그 전제를 뒤집었습니다.

- 캐릭터끼리 유저 없이 대화합니다
- 그 대화에서 유저 이야기가 나옵니다
- 유저는 그걸 **읽을 수만 있습니다** (입력 불가)

앱을 열면 내가 없는 사이에 두 사람이 나눈 대화가 쌓여 있습니다. 이름이 NULL인 이유입니다 — 유저는 이 세계에 존재하지만 값이 비어 있는 존재입니다.

| 방 | 구조 |
|---|---|
| 이재언 1:1 | 보건교사 29세. 감정을 업무로 번역하는 사람 |
| 이민현 1:1 | 고3 20세. 진심을 협박의 형태로 포장하는 애 |
| 단톡방 | 셋이 함께. 두 사람의 온도차가 드러나는 곳 |
| 두 사람 (관전) | 유저 없이 진행. `access denied` — 입력 불가 |

---

## 이야기의 구조 — 삼각형 셋

기능을 정하고 이야기를 붙인 게 아니라, 이야기가 요구하는 것을 기능으로 옮겼습니다.
아래 셋이 이 프로덕트의 설계도입니다.

### 1. 정보의 삼각형 — 누가 무엇을 아는가

| | 아는 것 |
|---|---|
| 이재언 | 유저와의 과거, 사탕 목걸이 |
| 이민현 | 삼각형 전체 — 삼촌의 마음, 자기 마음, 유저의 무지 |
| 유저 | 거의 없음 |

관계망의 중심에 있는 사람이 정보가 제일 적습니다. 두 사람 다 유저를 학교 밖에서
먼저 만났습니다 — 이재언은 20년 전에, 이민현은 개학 전 골목에서. **유저만 모릅니다.**

있지만 값이 비어 있는 존재. 이름이 NULL인 이유입니다.

### 2. 사탕의 삼각형 — 아무도 모르는 것

20년 전 공부방에서 다섯 살 여자애가 사탕으로 엮은 목걸이를 걸어주며 "행복해지라구"라고
했습니다. 그 애는 지금 교생이고, 그 말을 잊었습니다.

```
유저    발화자 · 잊음
  ↓
이재언  수신자 · 20년 보관
  ↓
이민현  수혜자 · 아무것도 모름
```

이재언은 단 걸 안 먹습니다. 커피는 블랙이고 누가 단 걸 주면 "됐어" 합니다. 본인은
취향이라고 생각합니다. 그런데 보건실 서랍의 사탕 통은 늘 채워져 있습니다 — 우는 애들,
주사 맞은 애들 주려고요. 이민현은 금연 중이라 입이 심심해서 그 서랍에서 사탕을
집어갑니다. 서랍이 왜 계속 채워지는지는 모릅니다.

**"행복해지라구"가 발신인 이름 없이 배달되고 있습니다.**

이건 설정이 아니라 제약입니다. 셋 중 누구도 전체 경로를 모르므로 **어떤 캐릭터도 이
구조를 대사로 말할 수 없습니다.** 모델에게 "말하지 마라"고 지시하는 대신, 아무도
모르게 만들어서 말할 수 없게 했습니다.

### 3. 시계로서의 떠남

교생은 한 달 뒤에 떠납니다. 셋 다 압니다 — 교생은 원래 가는 사람이니까요.
같은 시한이 셋에게 다르게 작동합니다.

| | 이 시한이 무엇인가 |
|---|---|
| 이재언 | 침묵의 마감. 말 안 하면 20년이 또 20년이 된다 |
| 이민현 | 시험. 예고된 이별을 유기와 다르게 통과할 수 있는가 |
| 유저 | 유저의 몫 |

이 시계는 화면에도 있습니다. 등록 화면의 `DAYS LEFT`는 유저가 채울 수 없는 칸이고,
방에 들어가면 직함 옆에 서른 칸짜리 막대가 하루씩 지워집니다. 숫자로 쓰면 읽고
지나가는데, 칸이 줄어들면 눈에 남습니다.

D-0이 되면 두 사람의 상태메시지가 바뀝니다. 대화를 몇 마디 했든 상관없이요.

---

## 핵심 설계

### 1. 정보 비대칭을 프롬프트가 아니라 아키텍처로 강제

캐릭터가 다른 방의 대화를 "아는 척"하지 못하게 만드는 것이 이 프로덕트의 핵심입니다. 프롬프트로 "인용하지 마라"고 지시하는 방식은 새어나갑니다.

그래서 **다른 방의 대화 원문을 서버로 아예 전달하지 않습니다.** 메타데이터만 보냅니다.

```json
"signals": { "minhyun": { "count": 12, "minsAgo": 5, "vibe": "들뜸" } }
```

캐릭터는 "저 방에서 오늘 대화가 많았고 방금까지 활발했다"만 압니다. 인용하고 싶어도 물리적으로 불가능합니다.

결과적으로 이민현은 유저의 기분 변화를 눈치채고 삼촌을 떠보지만, 무슨 대화가 오갔는지는 끝내 모릅니다.

### 2. 분위기 판정에 LLM을 쓰지 않음

`vibe`를 별도 LLM 호출로 판정하면 요청당 비용이 두 배가 됩니다. 규칙 기반으로 산출합니다.

| 조건 | 판정 |
|---|---|
| 느낌표·ㅋㅎ 비율 40% 초과 | 들뜸 |
| 물음표 비율 50% 초과 | 캐묻는 중 |
| 평균 길이 6자 미만 | 말이 짧아짐 |

정확도는 LLM보다 낮지만 **캐릭터 반응을 흔들기에는 충분**하고 비용은 0입니다. (앱에 구현. 웹은 `count`·`minsAgo`만 보냅니다.)

### 3. 자율 대화는 멀티에이전트가 아님

캐릭터 둘을 각각 LLM으로 돌려 핑퐁시키면 호출이 발화 수만큼 늘고 호흡이 끊깁니다.

**한 번의 호출로 두 사람의 대화 4~8발화를 통째로 생성**합니다. 소설가 한 명이 두 인물의 대사를 쓰는 방식입니다. 호출 수가 1/8로 줄고 주고받는 리듬이 자연스럽습니다.

### 4. 프롬프트 캐싱 — 방끼리 앞부분을 나눠 쓰게 끊음

시스템 프롬프트가 1:1 방 약 15,000자, 단톡 23,800자, 관전 22,000자입니다. 매 요청 재전송은 낭비입니다.

캐시는 **앞에서부터 바이트가 같은 만큼** 적중합니다. 그래서 고정부를 한 덩어리로 두지 않고, 여러 방이 공유하는 순서대로 셋으로 끊었습니다.

| 조각 | 내용 | 크기 | 공유하는 방 |
| --- | --- | --- | --- |
| ① 세계 | 세계관·삼각 구조 | 2,751자 | 네 방 전부 |
| ② 인물 | 이재언 / 이민현 설정 | 8,040~16,286자 | 재언방·단톡·관전이 앞부분 공유 |
| ③ 규칙 | 출력 형식·말버릇·사실·사진 목록 | 2,940~4,803자 | 방마다 다름 |
| ④ 가변부 | 관계 단계·유저 프로필·눈치 신호·최근 사진 제외·방금 받은 선물 | 130~280자 | 캐시 안 함 |

방을 옮겨도 앞 조각은 다시 청구되지 않습니다. 재언방 → 단톡방이면 10,791자(단톡 프롬프트의 45%), 단톡방 → 관전이면 19,037자(87%)를 읽기로 넘어갑니다.

수명은 `ttl: "1h"`입니다. 기본값 5분은 메신저와 맞지 않습니다 — 잠깐 딴 일 하다 다시 열면 매번 캐시를 새로 쓰게 됩니다. 쓸 때 2배(5분짜리는 1.25배)지만 읽을 때 1/10이라, 한 시간 안에 세 마디만 더 오가도 이득입니다.

가변 요소가 하나라도 앞 조각에 섞이면 그 뒤가 전부 다시 청구됩니다. 테스트로 고정한 이유입니다.

```
ok  신호·프로필·단계·선물이 달라져도 고정부는 그대로
ok  가변부는 맨 뒤다 — 앞에 끼면 뒤가 전부 캐시에서 빠진다
ok  재언방과 단톡방은 재언 설정까지 같이 쓴다
ok  단톡방과 두 사람 방은 인물 설정을 통째로 같이 쓴다
```

적중 여부는 오류로 드러나지 않고 조용히 정가로 청구됩니다. 그래서 진단 화면(`?diag=`)에 `usage`의 쓰기/읽기 토큰을 같은 방 두 번 호출로 찍어 봅니다.

### 5. 캐릭터 설계 — 태그가 아니라 방어 기제

"츤데레, 무뚝뚝함" 같은 성격 태그로 짜면 예시 대사 밖의 상황에서 무너집니다. NULL은 **압박받을 때 무너지는 순서**를 정의했습니다.

| 이재언 | 이민현 |
|---|---|
| **1 사무** — 모든 상황을 업무로 번역 | **1 장난** — 진심을 협박으로 포장 |
| **2 소거** — 번역이 안 되면 이름 붙기 전에 지움 | **2 확인** — 불안하면 "진짜죠?"로 처리 |
| **3 누출** — 소거가 실패하면 몸이 샘.<br>말은 건조한데 손이 먼저 움직임 | **3 철수** — 확인이 실패하면 조용해짐.<br>기척을 줄임 |

로버트 맥키의 캐릭터화/캐릭터 구분, K.M. 웨일랜드의 Ghost–Lie 체계를 프롬프트 구조로 옮겼습니다.

**검증 — 지시하지 않은 반응**

```
유저    선생님 혹시 저 어디서 본 적 있지 않아요?
이재언  …
이재언  아뇨.
```

침묵 후 부정. 이 캐릭터는 유저를 20년 전부터 알지만, 인정하는 순간 자기가 기억하고 있었다는 사실이 드러나므로 거짓말합니다. 예시 대사에 없는 반응입니다.

### 6. 오프닝 — 설명하지 않고 보여주기

이야기를 문장으로 설명하고 시작하면 프롤로그 나레이션이 됩니다. 대신 Y2K 데스크톱 한 장을 놓고,
흩어진 **가짜 오류창**이 대신 말하게 했습니다.

```
Error          이름을 입력해야 존재할 수 있어요.   [ok]
System error   당신을 찾을 수 없습니다.           [Cancel]
loading...     ▓▓▓▓▓▓▓▓▓▓
```

이름 칸이 같은 화면에 있습니다. **이름을 넣는 것이 그 오류를 지우는 일**이 됩니다.

넣고 나면 등록 화면이 나옵니다. 처음에는 여기도 읽기 전용이었습니다 — 이름과 역할이 자동으로
채워지고 4.6초 뒤에 넘어갔습니다. 그런데 부제가 *the blank u fill in*인 프로덕트에서
빈칸을 유저가 아니라 화면이 채우고 있었습니다.

```
SUBJECT     [ 국어 ] 과목 교생
AGE         [  28 ] 세
LIKES       [ 메롱 ] 를 좋아하고
HATES       [  오이 ] 를 싫어한다
DAYS LEFT   null
            ▓▓▓▓▓▓▓▓▓▓  READY ✓
```

지금은 네 칸을 직접 채웁니다. 채운 값은 그대로 서버로 가서 캐릭터가 알고 있는 정보가 됩니다.
시간이 지나도 안 넘어가고, **다 안 채워도 나갈 수 있습니다** — 비워두는 것도 이 이야기에서는 답입니다.

`DAYS LEFT`만 유저가 손댈 수 없는 칸으로 남겨뒀습니다. 넷을 다 채워 `READY ✓`가 떠도 그 줄은
`null`입니다. 한 달 뒤에 떠난다고 문장으로 말하는 대신, 유저가 채울 수 있는 칸과 채울 수 없는
칸을 나란히 놓았습니다. 숫자로 바뀌지 않게 테스트로 박아뒀습니다.

떠나는 날은 다른 데서 셉니다. `etc.`를 열면 `실습 D-30`이 있고, 첫 대화한 날부터 하루씩 깎입니다.

### 7. 데모 모드 — 키 없이 들어온 사람도 빈 화면을 보지 않는다

저장소 링크만 보고 들어온 사람에게 재시도 버튼만 보이면 아무것도 전달되지 않습니다.
API가 실패하면 그 턴은 문구집으로 메우고, **다음 전송에서 진짜를 다시 시도합니다** —
한 번의 429로 세션 전체가 각본으로 굳지 않게요. `?demo=1`로 계속 데모를 강제할 수도 있습니다.

조용히 가짜로 바꾸지는 않습니다. 실패 원인은 콘솔에 그대로 남고 하단 바에 `demo` 표시가 뜹니다 —
안 그러면 진짜 장애를 못 알아챕니다.

**문구집은 488개 의도 · 2,047줄입니다.** 손으로 고치는 건 `docs/dialogue-corpus.md` 한 파일뿐이고,
`tools/build-demo.mjs`가 웹용 `<script>`와 앱용 `.ts`를 같이 뽑습니다. 매칭 엔진은 두 결과물에
그대로 이어 붙기 때문에 웹과 앱이 다른 답을 할 수가 없습니다. 생성기는 원본과 줄 수가 맞는지
세어보고 안 맞으면 멈춥니다.

**고르는 순서는 여덟 단계입니다.** 위험·안전 → 직전 대답에서만 열리는 갈래 → 방 전용 →
완전 일치 → 여러 핵심어 → 단일 핵심어 → 짧은 입력 → 폴백. 흔한 낱말은 겹쳐도 단서가 아니므로
IDF로 무게를 매기고, 최근 다섯 번에 쓴 답은 빼고 고릅니다. 설렘 대사에는 쿨다운이 있고 —
매번 나오면 설렘이 아니라 배경이 됩니다 — 아프다고 한 직후에는 놀리지 않습니다.

**틀어진 자리는 전부 같은 얼굴을 하고 있었습니다. 그럴듯한 오답입니다.**

| 증상 | 원인 |
|---|---|
| "사랑해요"에 사과가 돌아옴 | 어간을 한 자까지 깎았다. 사랑→사(조사 "랑" 제거), 사과→사 |
| "뭐해"가 "뭐 해요?"에 안 걸림 | 쪼개면 "뭐"는 흔해서 버려지고 "해요"만 남아 영영 안 만난다 |
| "보고 싶어요"에 삼촌을 불러다 주겠다는 답 | "삼촌 보고 싶어요"의 일부인데, 빠진 두 글자가 대상을 바꾼다 |
| "라멘 좋아해?"가 고백으로 샘 | "좋아해"가 세 글자라 다른 말 안에 통째로 들어간다 |

고친 방식은 하나로 모입니다. **한쪽이 다른 쪽의 앞부분이면 같은 말로 보되 길이를 보고,
입력이 별칭보다 크면 그 얹은 것이 화제다.** 어간은 두 자 밑으로 안 깎습니다 — 한 자짜리
어간은 어차피 아무 말이나 걸립니다.

지금 문구집의 입력 1,075개를 전부 쳐보면 **1,065개가 제 답으로 갑니다.** 이 비율 자체가
테스트라, 매칭을 건드리면 여기가 먼저 떨어집니다.

**사진은 제 말풍선으로 나갑니다.** 처음에는 답의 마지막 줄에 붙였는데 "비 그치면 알려줘요"
밑에 비 사진이 걸렸습니다. 보내는 사람의 뜻이 아니라 화면 장식이 됩니다. 지금은 "지금요."
한 마디와 함께 따로 나가고, 연달아 보내지 않습니다.

**셀카는 민현만 보냅니다.** 처음부터 주면 그건 셀카가 아니라 프로필 사진이라, 가까워지기
전에는 미룹니다 — 미루는 말이 곧 아까워한다는 표시가 됩니다. 재언은 안 찍는 사람이라
거절하면서 대신 오라고 합니다.

**선물은 말이 아니라 물건입니다.** 이름을 "회색 머그컵 선물이에요"라는 문장으로 꾸며
매칭에 태웠더니 아무 데도 안 걸려서 못 알아들었다는 답이 나갔습니다. 지금은 열쇠를 그대로
넘겨 표에서 찾습니다.

**선톡은 목록으로 옵니다.** 방을 열어야 말을 거는 건 메신저가 아닙니다. 안 보고 있을 때
와야 안 읽음이 붙고, 안 읽음이 붙어야 열어볼 이유가 생깁니다. 한 번에 한 사람만 겁니다 —
두 사람이 같은 초에 말을 걸면 그건 사람이 아니라 알림입니다.

### 8. 상태메시지 — 나란히 놓으면 대화가 된다

예쁜 문구를 여러 개 돌려 쓰지 않습니다. 관계가 나아간 만큼만 바뀝니다.

둘 다 대놓고 상대를 지칭하지 않습니다. 평범한 공지처럼 써놓고 실제로는 한 사람만
알아듣게 합니다. 그래서 같은 시기의 둘을 나란히 놓으면 주고받는 말이 됩니다 —
정작 둘은 서로 보라고 쓴 게 아닌데.

| 대화 | 이재언 | 이민현 |
|---|---|---|
| 0 | 별일 없어요. | 수업 중. 아마도. |
| 16 | 문은 열어둘게요. | 기다리는 거 아니에요. |
| 40 | 어디 안 가요. | 그 말 취소하면 안 돼요. |
| 80 | 아직 남았어요. | 곧이잖아요. 지금이 아니라. |
| 120 | 남은 동안은 여기 있어요. | 안 알려줘도 알아요. |
| **D-0 · 안 왔다** | **잘 지내요. 항상.** | **모르는 걸로 할게요.** |
| **D-0 · 다시 왔다** | **아직 자리 있어요.** | **이제 와요?** |

마지막 두 쌍만 대화 수가 아니라 **시계**가 정합니다. 실습이 끝나는 건 몇 마디 했느냐와
상관없는 일이니까요. 대화 수(120)에 걸어놨더니 하루에 백스무 마디 하면 D-29에 작별
인사가 떴습니다.

D-0에서 멈춰만 두는 것도 틀렸습니다. 백 일째에도 「잘 지내요. 항상.」을 걸어놓고 계속
대화하면 **작별 인사를 붙여둔 사람과 무한히 이야기하는 화면**이 됩니다. 그래서 떠난
뒤를 둘로 나눴습니다 — 기준은 D-0 뒤에 **유저가** 말을 했느냐. 프로필만 열면 작별
인사고, 한 마디 하면 재회입니다. 새로 저장할 상태가 없습니다. 이미 갖고 있는
타임스탬프로 판정되고, 말을 거는 순간 저절로 넘어갑니다.

바뀐 것을 말풍선으로 알리지 않습니다. 그건 그 사람이 나한테 한 말이 아니니까요.
방 목록에서 아바타 둘레가 도는 것으로 알리고, 프로필을 한 번 열어보면 꺼집니다.

### 9. 지도와 가방 — 가는 곳을 유저가 정한다

처음엔 자리가 캐릭터의 초대로만 열렸습니다. 관계가 쌓이면 「옥상 같이 갈래요?」가 뜨고,
`갈게요`를 누르면 그 자리 사진이 깔리고 말풍선이 걷힙니다. 마주 앉은 걸 괄호 지문으로
때우던 것을 화면이 대신 말하게 한 겁니다.

그런데 가는 곳을 늘 저쪽이 정하니까 **귀양 가는 기분**이 됐습니다. 그래서 반대편을
만들었습니다. `map` 탭에서 유저가 자리를 고르고 누굴 부를지 정합니다.

| | 정하는 쪽 | 열리는 조건 |
|---|---|---|
| 초대 | 캐릭터 | 그 방 대화 수. 거절하면 다시 안 나옴 |
| 지도 | 유저 | **다녀온 자리** |

대화 수로 열지 않았습니다. 앉아서 말만 쌓아도 지도가 넓어지면 그건 지도가 아니라
그냥 또 하나의 게이지입니다. **다녀와야 다음이 열립니다.**

```
교실 ┐
     ├→ 옥상 ┬→ 편의점 → 레코드샵 ┐
보건실┘        └→ 도서관 → 빨래방  ┴→ 집
```

처음 열려 있는 건 **교실과 보건실** 둘뿐입니다. 학교 안이니까요. 둘 다 다녀와야
옥상이 열리고 — 학교에서 유일하게 둘 다 오는 자리라서 — 거기서 민현 쪽과 재언 쪽으로
갈라집니다. 마지막 「집」은 양쪽을 다 걸어봐야 열립니다. 한쪽만 파서 남의 집에 갈
수는 없어야 하니까요.

자리마다 받아오는 것이 하나씩 있습니다. 편의점에선 하리보, 도서관에선 빌린 책,
레코드샵에선 중고 LP. 받은 건 `bag`에 쌓입니다 — `gift`가 준 것이면 `bag`은 받은 것이고,
둘이 나란히 있어야 주기만 하는 앱이 안 됩니다.

**무엇을 줄지는 서버가 정하고, 언제 줄지만 모델이 정합니다.** 물건까지 모델에게 맡기면
매번 다른 걸 지어내서 가방에 넣을 수가 없습니다. 그렇다고 도착하자마자 쥐여주면
자리에 간 이유가 물건 수령이 됩니다. 그래서 서버는 「이 자리에서 하리보를 건넨다,
건네는 턴에 `"give": "haribo"`를 쓴다」까지만 말하고 시점은 대화를 보는 쪽이 정합니다.

그리고 **안 건네고 끝나는 턴을 위해 자리에서 나올 때 채워줍니다.** 받는 순간은
모델에게 맡기되 받는다는 사실까지 맡기지는 않습니다 — 그러면 가방이 비고, 지도를
돌 이유가 사라집니다.

빌린 책에는 `빌린 것` 표시가 남습니다. 돌려줄 게 하나 남아 있어야 다시 만날 이유가 됩니다.

---

## 모델이 형식을 어겨도 화면은 깨지지 않는다

프로덕션에서 실제로 터진 것들입니다. **전부 회귀 테스트로 고정돼 있습니다.**

**단톡방 말풍선 뭉침** — 단톡·관전방은 이력을 `[이재언] 말` 형태로 모델에게 줍니다. 모델이 JSON 대신 **그 형식을 그대로 따라 썼고**, 파싱이 실패해 세 사람 대사가 말풍선 하나로 떨어졌습니다. 화자도 엉뚱하게 붙었고요.

→ 이름표가 붙은 줄을 화자별로 풀어주고, 그 방에 없는 사람은 말하지 못하게 막았습니다. 프롬프트에도 "이름표는 읽으라고 붙인 것이지 답하는 형식이 아니다"를 명시했습니다.

**말버릇이 화면을 뒤덮음** — 이민현이 모든 줄을 `...`으로 시작했습니다. 말버릇 필터는 **말풍선 단위**로 거르는데 그 점들은 말풍선 *안의 줄*이라 필터가 손도 못 댔습니다.

→ 줄바꿈이 든 말풍선을 먼저 쪼갠 뒤 필터를 태웁니다. 필터가 닿는 단위를 맞추는 것이 핵심이었습니다. 말끝이 흐려지는 `"그게..."`는 말투이므로 남깁니다.

**사진 키워드 오발** — "음악 추천해줘"에 이어폰 낀 사진이 붙었습니다. 키워드 매칭 폴백이 원인이라 걷어냈습니다. 지금은 모델이 맥락을 보고 고른 것만 나가고, 화이트리스트 검증·중복 방지·응답당 1장 제한이 서버에 있습니다.

**JSON 3단 폴백** — 정규식 추출 → 이름표 파싱 → 원문. 어느 단계에서 걸리든 사용자에게 JSON 원문이 노출되지 않습니다.

---

## 기술 구성

```
[웹]  GitHub Pages ── React (단일 HTML, 빌드 없음)
                          │
                          ▼
[API] Cloudflare Workers ── Claude API (운영 모델)
                          │
[앱]  Expo / React Native ── SQLite (로컬 영속)
                          └ Cloudflare R2 (오디오)
```

**백엔드 (Cloudflare Workers)**

- **모델 폴백 체인** — 최상급 → 차상급 → 상급 순(정확한 ID는 `worker.js`의 `MODELS` 표). 400/404는 다음 모델로, 401/429/5xx는 즉시 중단. 성공한 모델을 기억해 재사용
- **실행 지역 고정** — `placement: { mode: "targeted", region: "azure:koreasouth" }`
- **출처 제한 + 레이트리밋 + 진단 잠금** — 아래 참조

**앱 (Expo / React Native)**

- **SQLite** — 대화 영속 저장. 눈치 신호, 프로필 단계, 해금 조건이 전부 쿼리 기반
- **타이핑 연출** — 글자 수 비례 딜레이에 캐릭터별 차등 (이재언 60ms/자, 이민현 35ms/자). 성격을 UI 레이어에서 한 번 더 표현
- **프로필 시스템** — 상태메시지·배경·BGM이 **5단계**로 교체. 단계는 누적 대화량과 **날짜를 둘 다** 넘어야 오른다(0/16/40/80/120 대화 · D-30/26/20/12/5). 유저는 하루에 백 개씩 보내므로 대화 수만 보면 첫날 밤에 마지막 단계가 된다 — 화면에는 D-29라고 적혀 있는데. 1일은 1일이어야 한다. 인물마다 곡이 넷이고 배경과 같이 해금됩니다. 유저가 안 볼 때 조용히 바뀌고, 방 목록의 아바타 둘레가 도는 것으로만 알립니다
- **선물** — 장바구니에서 검색해 캐릭터에게 보냄. ♡는 주고받은 말에서 나옵니다(열 마디에 하나). 40번을 넘긴 뒤 준 선물은 **그 캐릭터의 프로필 배경**이 됩니다

웹과 앱은 카탈로그를 각자 들고 있습니다. **한쪽만 고쳐 어긋나는 것이 이 프로젝트에서 가장 자주 난 사고**라, 두 파일을 실제로 읽어 대조하는 테스트를 넣었습니다.

---

## 겪은 문제와 해결

### Workers 실행 지역으로 인한 403

배포 후 한국에서 접속할 때만 Anthropic API가 403을 반환했습니다. 같은 요청을 해외에서 보내면 정상이었고, 키를 재발급하고 코드를 바꿔도 재현됐습니다.

원인은 Workers가 요청자와 가까운 엣지에서 실행되는 구조였습니다. 한국 유저의 요청이 인근의 미지원 지역 엣지에서 실행되면서 아웃바운드 IP가 차단된 것이었습니다. Placement에서 실행 지역을 고정해 해결했습니다.

**같은 코드, 같은 키인데 요청 위치에 따라 결과가 달랐습니다.** 코드가 아니라 실행 환경을 의심해야 하는 문제였습니다.

덧붙여, `region`은 `provider:region` 형식이어야 합니다. `koreasouth`만 쓰면 배포가 검증 오류로 실패하는데 **대시보드에는 이전 버전이 계속 떠 있어** 몇 시간 동안 "코드를 고쳐도 반영되지 않는" 상태를 겪었습니다. 배포 로그를 봤어야 했습니다.

### 공개된 백엔드로 토큰이 새던 경로

포트폴리오에 "CORS 제한·진단 엔드포인트 잠금"이라고 적어놓고 실제로는 `ALLOW_ORIGIN = "*"`였고, **진단 엔드포인트가 GET 아무 요청에나 열려 있었습니다.** 그 진단은 모델 세 개를 실제로 호출합니다. 주소를 브라우저로 열기만 하면 토큰이 나갔다는 뜻이고, 크롤러도 마찬가지였습니다.

- 진단은 `DIAG_TOKEN`이 맞을 때만 모델을 부릅니다. 기본값은 호출 없는 간이 점검 — **안전한 쪽이 기본값**
- POST는 출처 허용 목록 + IP당 분당 20회

**한계를 분명히 해두면:** 앱은 `Origin`을 안 보내므로 통과시켜야 하고, 따라서 CORS로 `curl`은 막지 못합니다. 레이트리밋은 아이솔레이트 메모리에 살아서 정확한 상한이 아닙니다 — 정확히 세려면 KV나 Durable Object가 필요합니다. **둘 다 인증이 아닙니다.** 정적 사이트에 심는 키는 어차피 공개됩니다.

구현 중에도 하나 잡았습니다. 가드를 키 검사 **뒤에** 뒀더니 키가 없을 때 500이 먼저 나가 차단이 아예 작동하지 않았습니다. 테스트가 403 대신 500을 잡아냈습니다.

### Expo Go로는 되는데 APK만 켜자마자 죽던 것

APK가 설치도 되고 아이콘도 뜨는데 실행하면 즉시 종료됐습니다. 같은 코드가 Expo Go(QR)로는
멀쩡히 돌았습니다.

**그게 오히려 범위를 좁혀줬습니다.** Expo Go는 자기가 버전을 맞춰 넣어둔 네이티브 모듈을 쓰고
JS만 내 것을 돌립니다. APK는 `package.json`에 적힌 대로 네이티브를 직접 짓습니다. JS가 아니라
껍데기 문제라는 뜻이었습니다.

여기서 `newArchEnabled`를 껐습니다. 흔한 원인이라 찍은 건데 틀렸습니다. **로그를 봤어야 했습니다.**

```
FATAL EXCEPTION: create_react_context
java.lang.NoClassDefFoundError: expo/modules/kotlin/types/AnyTypeCache
  at expo.modules.asset.AssetModule.definition(AssetModule.kt:125)
```

`expo-doctor`가 이유를 말해줬습니다. `expo-asset`이 **두 버전** 깔려 있었습니다 — SDK 57짜리와
`expo` 본체에 딸린 SDK 54짜리. `expo-audio`가 `expo-asset`을 peer dependency로 요구하는데
`package.json`에 선언이 없어서, npm이 SDK를 안 보고 최신을 끌어온 것이었습니다. 빌드는 둘 중
하나만 넣을 수 있고 57을 골랐는데, 57짜리 `AssetModule`이 찾는 클래스가 54 코어에는 없습니다.

`npx expo install expo-asset expo-constants`로 SDK에 맞는 버전을 정식 등록하고 `node_modules`를
지우고 다시 깔아 해결했습니다. `expo install --fix`는 `package.json`에 적힌 것만 보기 때문에
**선언 자체가 없는 이 경우를 잡지 못했습니다.**

교훈은 두 개입니다. 네이티브가 낀 문제에서 증상으로 원인을 찍는 것은 로그 한 번보다 항상 느립니다.
그리고 Expo 프로젝트에서 네이티브 모듈은 `npm install`이 아니라 `expo install`로 깔아야 합니다.

### 최상급 모델의 temperature 미지원

모델 업그레이드 후 400이 났습니다. `temperature`가 거부되는 것이 원인이었습니다. 제거하고 문체의 변주는 시스템 프롬프트가 담당하도록 이관했습니다. 이 경험이 모델 폴백 체인을 만든 계기입니다.

---

## 테스트

```
node test/run.mjs
```

의존성·네트워크·API 키 없이 돕니다. 모델을 부르지 않고 검증 가능한 것만 다룹니다. (503개)

| 묶음 | 내용 |
|---|---|
| 말풍선 파싱 | 모델이 형식을 어겨도 화면이 깨지지 않는가 |
| 말버릇 | 필터가 닿는 단위로 쪼개지는가 / 말투는 남는가 |
| 사진 | 지어낸 키·중복·개수 제한 |
| 프롬프트 캐싱 | 고정부가 매 턴 동일한가 |
| 백엔드 잠금 | 출처·레이트리밋·진단 토큰 |
| 웹·앱 대조 | 두 클라이언트가 같은 세계를 보는가 — 갤러리·등록 칸·영상 컷·etc. 문구·실습 D |
| 데모 모드 | 키 없이 들어온 사람도 빈 화면을 보지 않는가 |
| 문구집 매칭 | 입력 1,075개가 제 답으로 가는가 · 틀어졌던 자리들 |
| 상태메시지 | 워커·웹·앱 셋이 같은 문구를 쓰는가 · 떠난 뒤는 시계가 정하는가 |
| 앱 레이아웃 | 헤드리스로 못 돌리는 것은 소스로 막는다 |

여기 모인 것은 전부 **실제로 한 번 터졌던 것**입니다. 새 기능을 넣을 때가 아니라 화면이 깨졌을 때 하나씩 추가했고, 그래서 테스트 이름이 증상으로 붙어 있습니다.

---

## 남은 것

- 관전방 백그라운드 자동 생성 (Cron Trigger) — 앱을 안 여는 동안에만 대화가 쌓이는 규칙
- 날씨 API 연동 아침 안부 — 정보가 아니라 걱정으로 전달되는 알림
- 바깥 세계 연결 — 하루 1회 검색 후 KV 저장, 캐릭터 취향 필터를 통과시켜 반응

---

## 이 프로젝트로 말하고 싶은 것

AI 프로덕트에서 어려운 건 모델을 부르는 게 아니라, **무엇을 주지 않을지** 정하는 것이라고 생각합니다.

NULL의 핵심 기능은 전부 "안 주는 것"으로 만들어졌습니다. 다른 방의 대화를 안 준다(정보 비대칭). 관전방에 입력을 안 준다(관찰자 위치). 캐릭터가 유저의 과거를 안 밝힌다(방어 기제). 분위기 판정에 LLM을 안 쓴다(비용 설계).

제일 아끼는 건 사탕의 삼각형입니다. 그건 프롬프트에 "말하지 마라"고 적어서 지켜지는 게 아니라, **아무도 전체를 모르게 설계했기 때문에** 지켜집니다. 말하지 말라고 하면 언젠가 말하지만, 모르는 것은 말할 수가 없습니다.

기능을 더하는 것보다 덜어내는 판단이 프로덕트의 성격을 만들고, 그 판단을 **프롬프트가 아니라 코드 구조로 강제하는 것**이 설계라고 생각합니다.

---

캐릭터 일러스트 AI 생성(ChatGPT) · 프로필 BGM AI 생성(Suno, 가사 직접 작성) · 개발 도구 Claude Code
